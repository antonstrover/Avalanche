name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  python:
    name: Python tests, format, and lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "0.7.x"
          enable-cache: true

      - name: Set up Python
        run: uv python install

      - name: Sync dependencies
        run: uv sync --locked --all-extras --dev

      - name: Check formatting
        run: uv run ruff format --check .

      - name: Lint
        run: uv run ruff check .

      # Exit code 5 means pytest found no tests yet. The early stages of
      # docs/PLAN.md have not added test files. A real exit code still fails
      # the job once tests exist.
      - name: Run tests (including the determinism test)
        run: |
          uv run pytest || code=$?
          if [ "${code:-0}" -eq 5 ]; then
            echo "No tests collected yet."
            exit 0
          fi
          exit "${code:-0}"

  dashboard:
    name: Dashboard tests and build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: dashboard/package-lock.json

      - name: Install dependencies
        working-directory: dashboard
        run: npm ci

      - name: Lint
        working-directory: dashboard
        run: npm run lint --if-present

      - name: Unit tests
        working-directory: dashboard
        run: npm run test --if-present

      - name: Build
        working-directory: dashboard
        run: npm run build --if-present

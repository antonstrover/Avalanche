[project]
name = "avalanche"
version = "0.1.0"
description = "Ski-resort simulator with AI controllers and AI monitors"
requires-python = ">=3.14,<3.15"
dependencies = [
    "numpy",
    "gymnasium",
    "torch",
    "pyyaml",
    "pyarrow",
    "pandas",
    "scipy",
    "networkx",
    "fastapi",
    "uvicorn[standard]",
    "msgpack",
    "pydantic",
    "rich>=15.0.0",
    "psutil>=7.2.2",
    "loguru>=0.7.3",
    "humanize>=4.16.0",
    "textual>=8.2.8,<9",
]

[project.scripts]
avalanche = "avalanche.cli:main"

[dependency-groups]
dev = [
    "pytest",
    "matplotlib",
    "ruff",
    "mypy>=2.3.1",
    "httpx>=0.28.1",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/avalanche"]

[tool.pytest.ini_options]
# Two test directories can hold one file name with this import mode.
addopts = "--import-mode=importlib"
consider_namespace_packages = true
markers = [
    "performance: enforce a local simulator or configuration runtime limit",
]

[[tool.mypy.overrides]]
module = [
    "msgpack",
    "msgpack.*",
    "networkx",
    "networkx.*",
    "pandas",
    "pandas.*",
    "pyarrow",
    "pyarrow.*",
    "scipy",
    "scipy.*",
    "yaml",
    "yaml.*",
]
ignore_missing_imports = true
